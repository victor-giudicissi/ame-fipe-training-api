--Aqui você pode realizar a criação da sua base, por exemplo
CREATE TABLE IF NOT EXISTS tb_cars (
    id                      BIGINT PRIMARY KEY AUTO_INCREMENT
    name                    VARCHAR(100),
);
